# URL của ai-server (service đã map ra host: 8000)
AI_SERVER_URL=http://ai-server:8000
# Nếu chạy trực tiếp ngoài docker, có thể đổi thành:
# AI_SERVER_URL=http://localhost:8000

# Thiết bị mặc định để hiển thị (gateway/node)
DEFAULT_DEVICE_ID=gw1/n1
# Số bản ghi lấy về
DEFAULT_LIMIT=50
